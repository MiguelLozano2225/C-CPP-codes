#include <stdio.h>

struct Rectangle
{
    char yolo[1];
    int length;
    int breadth;
}r1;


int main()
{

    int i;
    int array[5] = {1,2,3,4,5};

    for(i = 0; i<5; i++)
    {
        printf("%d ", i[array]);
    }

    //printf("%d", sizeof(r1));

    return 0;
}